<resources>
    <!-- Base application theme. -->
    <style name="ColineTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/ColineActionBar</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/ColineActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="ColineActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/blue_main</item>

        <!-- Support library compatibility -->
        <item name="background">@color/blue_main</item>
    </style>
</resources>
